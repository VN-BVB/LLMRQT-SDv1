chunk
固定长度 0.3
计算相似句子的相似度，然后作为chunk 0.5
small to big retrival 0.85
先检索大块的 Summary，锁定范围后，再在对应 Summary 下检索精细的 Chunk。 0.84


给chunk生成标题 0.5


检索到chunk后自动带出物理上的前后n个chunk补充上下文 0.6
索引阶段 让llm针对每个chunk预生成几个可能的问题 0.8

query 
查询重写，回溯提示、子查询分解 0.5

检索

reranker
cross encoder 0.7

rse滑动窗口 0.8

self rag 0.6

向量检索与 bm25关键词检索 通过rrf算法进行结果融合：已实现
100题结果：Dense Hit@5 64%，BM25 76%，RRF 81%，RRF+Reranker 83%；完整检索延迟 433.04 ms/query。

纠错相关度策略






先检索大块的 Summary，锁定范围后，再在对应 Summary 下检索精细的 Chunk。 0.84
让llm针对每个chunk预生成几个可能的问题 0.8
建立三层关系，父，子，问（父子）
关键词搜索与语义搜索
reranker
cross encoder 0.7

rse滑动窗口 0.8

向量检索与 bm25关键词检索 通过rrf算法进行结果融合 0.83 


FAISS 是一个向量相似度检索库，全称是 Facebook AI Similarity Search。
专门用来存 Embedding 向量，并快速找出“和问题最相似的文档片段”的工具。

后续统一使用 OHR-Bench；CMRC 只保留为小规模中文冒烟对照。
