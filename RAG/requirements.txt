# 当前 Linux + NVIDIA 环境使用 FAISS GPU；纯 CPU 机器请改回 faiss-cpu>=1.8.0。
# 为了让这个目录也能在一个全新的 Python 环境中使用，这里把直接依赖全部列出。
faiss-gpu==1.15.1; platform_system == "Linux" and platform_machine == "x86_64"
numpy>=1.26.0
torch>=2.2.0
transformers>=4.44.0
tqdm>=4.66.0
pypdf>=5.0.0
python-docx>=1.1.0
pyarrow>=15.0.0
bm25s>=0.3.11
jieba>=0.42.1
